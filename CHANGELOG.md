# Changelog

## 开发中

- PAP 骨骼动画采样与前端播放：数据层新增 `animation.rs`（pap 容器头解析 + 内嵌 havok tagfile，animations/bindings 平行数组按名表 `havok_index` 关联，track→bone 经 `binding.transform_track_to_bone_indices` 映射到本骨架，越界 track 丢弃计数；加载时按量化直方图预检剔除采样器不支持的动画，vendored 解析 panic 全部 catch_unwind 转错误），`sample_animation_pose`/`animation_joint_matrices` 采样到 SkeletonPose/关节矩阵（时间毫秒、越界钳制），角色 action.pap + 常用 emote、monster/demihuman mount/idle pap 多候选探测（命中合并、全缺返回空集不报错）；模型预览页（宠物/坐骑）与角色页右侧栏新增「动画」下拉（None=rest + 各动画名），选中后 rAF 循环 `t=(now-start)%duration` 采样并增量上传关节矩阵，切换动画/模型重置计时，无动画集时 UI 不出现、渲染行为不变。修复 vendored havok 样条求值的 knot 窗口重建 bug（上游仅在 span==p 的首段正确，中段四元数爆成非单位值导致姿态翻转）与顶点 blend 索引语义（bone_table 绝对下标，此前按 submesh 窗口偏移重映射在非 rest 姿势下撕碎多窗口网格）；web 画布 device limits 补 storage buffer 蒙皮下限（WebGPU 后端均满足），动画播放时预览强制原位布局（平铺偏移量在蒙皮前顶点位置里，动画下会随关节旋转）。
- 骨骼 rest pose 与 GPU 蒙皮管线：数据层新增 `skeleton.rs`（sklb 解析容错 `blks`/`sklb` magic 与多版本头，vendored Havok 解析 panic 全部 `catch_unwind` 转错误；骨架不走 serde/IndexedDB，随加载内存存活），`load_chara_model_with_skeleton_from_resource`/`load_character_assembly_with_skeleton_from_resource` 平行入口随模型返回 rest pose 骨架（sklb 缺失静默降级）；渲染层 `create_model_with_skeleton` 构建实例 joint 表（mesh bone_table 名并集按名匹配）+ 实例级 joint storage buffer（256 上限）+ `update_joint_matrices` 姿势覆盖，WGSL 蒙皮 `Σ w·J·pos`（权重按和归一、法线 mat3(J) 近似）；rest pose 蒙皮输出与非蒙皮路径浮点噪声级一致（joint 数 0 旧分支零扰动），陆行鸟/中原男装配实测通过，颈部旋转冒烟肉眼可见。

- 新增「角色」页（角色组装器）：选择种族/部族/性别后以该组默认捏脸为基底，编辑脸型、发型、眉/眼/鼻/嘴/轮廓、身高/体格/胸围滑条、面部特征位与肤色/发色/挑染/瞳色/唇色/特征色/面妆色色板（数据源 character-make/character-palette 资产），捏脸以 26 字节 hex 同步到 `#/character?c=` 链接；右侧经本地游戏目录实时装配角色模型预览（部件原位重叠、attribute 按名启用——裸装含 `atr_lod` 在内恒开（刘海/尾巴中段等几何都在该子网格）、`atr_fv_*` 脸部特征件按面部特征字节逐位开关、敖龙角与鳞片正确显示，角色渲染色按 shader 家族写入材质），附简版模型统计。
- 角色拼装渲染点亮：默认捏脸 + human.cmp 调色板解析出角色渲染色（`CharacterAppearanceColors`：肤色/唇色+唇釉/发色/挑染/左右眼色+角膜环/特征色/面妆色 + lipstick/highlights/面妆镜像开关），加载时按 shader family 写入部件材质（skin 全收、hair 收发色、iris 收眼色、charactertattoo 收特征色，装备 character 等不受影响）；skin/hair/iris/charactertattoo shader 家族新增渲染分支（肤色乘算、唇妆混入、发色乘算+挑染、虹膜乘色、面纹 OptionColor），面妆 decal 贴图按真实数据探测的路径规律（`chara/common/texture/decal_face/_decal_*.tex`，BC4 贴图新增解码）加载并叠乘，缺失时静默降级。皮肤族材质按介电质处理（不再把 mask 数据通道误作金属度，皮肤不再渲染成金属质感）；头发以 mask R 通道作明暗渐变、G 通道作挑染区域，发色不再偏色发橙；发丝透明度改从法线贴图 Alpha 通道读取，并按 4x4 screen-door 抖动做 alpha clip（此前恒不透明/整片半透明/硬阈值削碎的问题全修，刘海等 atr_lod 子网格几何也恢复显示，头发不再秃碎）。
- 跨族回退的种族骨变形：回退文件网格（维埃拉/硌狮小衣来自猫魅/鲁加、裸肤手足来自中原/拉拉男、共享发型来自中原）按源族骨架 rest → 自身骨架 rest 逐骨变形烘焙（骨按名映射、顶点按蒙皮权重加权），比例与手部错位修正；游戏的 PBD 运行态骨变形以骨架替换近似，身高/体格/胸围的 RGSP 缩放仍未应用。
- 模型预览布局新增原位模式：`component_preview_layout = false` 时多 MDL 部件不再平铺拆开而是按游戏坐标重叠（角色拼装使用），默认行为不变。
- 数据层新增完整角色拼装（chara/human + 小衣装备域）链路：捏脸 26 字节结构（`CharacterCustomize`，含校验与 c 编码）、部件路径构建（游戏裸装 = 小衣 e0001 皮肤/内衣 top/dwn（sho 部分族有）+ 裸肤 e0000 手（足），硌狮/维埃拉无自身小衣文件按种族骨变形树回退、裸肤手足按版型回退中原同性别/拉拉男，脸/发/尾/兔耳含高地借脸与编号基数换算）、皮肤肤族材质表（精灵/猫魅→中原、鲁加女→高地女、拉拉女→拉拉男，跨族回退仍用角色自身肤族材质）、头发材质共享表与 human 域材质特例候选（脸与兔耳无版本目录/共享根改写）、加载入口 `load_character_assembly_from_resource`（对齐武器/装备/家具/宠物坐骑风格，sho/尾/兔耳可选探测合并，多 MDL 拼原位）；attribute 显隐按名判定（submesh 位是各 MDL 本地 attribute 表序、跨模型数值不可比），并暴露可用选项。
- 新增捏脸数据资产：`character-make.json`（32 组默认捏脸与捏脸菜单，CharaMakeType + CharaMakeCustomize 生成，发型/面妆选项索引表）与 `character-palette.json`（human.cmp 全色板：眼/挑染/唇/特征/面妆公共区 + 32 组肤色/发色，RGBA 非 squared），随资源缓存体系分发；xtask 新增 `--character-make`/`--character-palette` 生成子命令。修正 CharaMakeCustomize 提取的槽映射：真实结构为 18 槽（槽序即 c 编码序，key 步长 130，选项按 Data 序号排序），替换此前"32 组 × 125 行"的错误假设——旧提取下 4 组默认发型指向不存在的文件（敖龙女 h0024、北洋鲁加男 h0130、硌狮女 h0028/h0008），其馀多组选项列表错位；32 组全量真实装配守护测试现已覆盖。

## 2026-09-11

- 侧边栏展开宽度由 240px 收窄至 192px，减少导航标签两侧的空白。
- GitHub 仓库入口移至各功能页标题行右侧（与页面标题同处一行）：桌面端不再保留单独的顶栏，移动端位于顶部标题行；侧边栏底部只保留设置入口。
- 模型预览布局紧凑化：渲染控制（调试模式、Glass、Normal/Bloom/Flip Y 等）从画布悬浮层移入右侧信息栏，画布不再被遮挡；右侧面板收窄并收紧行距，左侧物品栏收窄栏宽、压低搜索框与筛选按钮高度、压缩物品行高，物品 ID 并入名称行以小字灰显，为模型画面腾出更多空间。
- 模型预览切换物品/种族/shape 不再销毁重建 WebGPU 画布：渲染器拆分为模型无关的常驻渲染上下文（设备、管线、后处理）与按模型同步创建的 GPU 实例，切换时只重建实例，不再重复初始化设备与重编译全部渲染管线；加载中与读取失败改为画布上的浮层，视角仅在物品或种族切换时重置，shape 切换保持当前视角。
- 武器模型页升级为“模型预览”：覆盖武器、防具与饰品，条目来自图鉴目录，支持按部位分组筛选、种族模型选择与双通道染色，腰带与复合部位给出暂不支持提示；新增 `#/equipment-models` 路由，原 `#/weapon-models` 链接与 `?item=` 参数保持可用。数据层新增任意装备 3D 渲染加载基础：按套装、种族与部位解析防具/饰品的模型与材质候选路径（含种族回退与材质版本回退），装备 ModelMain 按装备位段语义解码，加载入口与武器流程平行并复用材质、染色管线。
- “模型预览”新增家具/庭具：分组筛选加入家具与庭具，物品卡片与详情展示类别（室内家具/庭具）与模型 id，选中后经本地游戏目录解析 housing SGB 及其递归引用的全部 MDL 合并渲染（家具无染色与种族维度）。数据层内置家具目录（室内家具与庭具共 2000 余条，含物品名称、图标与模型 id，随资源缓存体系分发与更新），材质优先按 SGB 引用文件列表精确匹配、缺失时按模型目录推导。
- “模型预览”新增宠物/坐骑：分组筛选加入宠物与坐骑，物品卡片与详情展示类别与模型文件名（monster/demihuman），选中后经本地游戏目录加载模型（monster 单 MDL，demihuman 坐骑按槽位探测合并多 MDL，以绑定姿势静态预览，无染色与种族维度）。数据层内置目录覆盖全部宠物与坐骑解锁物品（900 余条，含物品名称、图标与 ModelChara 模型三元组，随资源缓存体系分发与更新），材质按多版本目录回退。
- 修复带属性部件（attribute submesh）的模型（如自走人偶001号）变体部件与本体重叠渲染的问题：游戏内属性部件默认全部未启用，此前预览把全部变体同时画出；现按游戏默认隐藏。右侧面板新增“部件变体”勾选列表，可逐个打开这些属性部件（默认全关，对应游戏默认状态）；列表下方带“仅显示选中部件”隔离开关，可隐藏无属性本体、单独查看选中的变体部件（部件按骨骼绑定姿势静态显示，与本体重叠是数据的真实状态）。
- 修复可染色家具（如海滨圆桌）部件渲染缺失成洞的问题：bg 变色材质（bgcolorchange/bgcrestchange）漫反射纹理的 alpha 通道是染色遮罩而非透明度，此前被误判为透明混合导致非染色部件（桌腿、镶边等）被穿透，现按不透明渲染。
- 修复模型渲染器为每个绘制批次重复上传整套材质纹理导致的显存爆炸：同一模型的全部批次现在共享按源纹理与采样语义去重的 GPU 纹理，多 submesh 模型（如陆行鸟坐骑）预览不再触发 wgpu Out of Memory 崩溃且显存占用大幅下降，渲染结果不变。
- 合成图画布组件抽取为独立开源仓库 [dioxus-flow](https://github.com/AzurIce/dioxus-flow) 并切换为 git 依赖；平移拖拽改为指针事件中同步直写 DOM，画布移动更加跟手。

## 2026-07-26

- 重做制作笔记为紧凑的可拖拽物品树，条目整行均可选择并以更醒目的物品图标显示名称和数量摘要，支持 Ctrl/Shift 多选并持久化目录展开状态；数量步进、重命名、删除及目录新增操作集中到右键菜单，拖拽可按条目的前、目录内、条目后三种落点跨层级整理，插入线随目标父级缩进并以层级引导线明确展示树结构；右侧制作分析由物品树选择自动决定单项或合并结果，合成画布和可滚动材料清单直接占据详情区域，画布支持以指针为中心的平滑缩放、自由平移及节点拖动；晶石改为紧凑的图标数量摘要，采集、兑换和市场均优先展示最终总消耗，并可按需展开来源、报价与方案明细，旧版目录与合成数据会自动迁移为物品条目。
- 制作清单物品树增加 Universalis 估价：物品条目分两行显示 NQ/HQ 的价格、区域日均销量与最近一周销量趋势，并排除展示架交易对价格和销量的干扰；右侧对齐相对更新时间和刷新入口，目录可递归汇总全部后代、在后代变化时即时重算并支持整组刷新。
- 市场估价改用应用级共享持久化缓存，并统一为“缓存即时显示 + 过期后台刷新”：制作清单、合成估价、材料分析与物品树先展示 IndexedDB 中的已有报价，超过有效期（价格 30 分钟、销量 12 小时）的报价后台批量更新，首次缺失自动补充，网络失败保留旧报价；材料清单头部和市场购买区块新增估价徽标与手动刷新按钮，可绕过有效期强制更新。
- 合成图新增“直接购买更划算”提示：当某个可合成节点（含清单成品）的全部材料按当前来源都是市场购买、且直接买它比分解更便宜时，其折叠按钮高亮并带角标，悬浮显示买价、材料成本与预计节省；是否折叠完全由用户决定，功能本身不改变任何清单数据。
- 材料清单来源汇总徽标统一为带语义配色的紧凑样式（采集绿、兑换紫、商店琥珀、市场蓝，与合成图节点配色一致），“市场优先”开启时显示高亮状态。
- 制作材料默认仍优先使用采集、商店与兑换，市场作为可交易材料的兜底来源；新增可撤销的“市场优先”模式，手动调整来源时会固化当前市场方案后退出，便于成本与收益测算。
- 修复调整材料来源或切换“市场优先”后，合成图节点颜色不随之更新的问题。
- API Bridge 连接地址不再限制为本地回环主机，保存后即可供物品与图鉴功能使用；设置页移除连接测试、资源测试和重复的工具工作区入口。
- 图鉴页拆分为“装备”和“解锁”两个顶层 Tab；全局搜索移动到页面标题区，装备可按职业、部位、装备等级和品级组合筛选。
- 装备同步增加确认预览，明确列出将新增和移除的条目，并保留解锁类图鉴状态不变。
- 图鉴库存同步支持识别尚未拆封的套装物品，并将其展开为套装内的全部装备，避免整套装备被误判为未持有。
- 优化合成笔记 flow 画布的平移、缩放渲染路径，并为所有弹窗补充 Enter 确认与 Escape 取消/关闭快捷键。
- 限制应用壳的整体高度，首页等长内容改为右侧内部滚动，侧边栏底部设置入口不再被撑出屏幕；合成检索的物品树折叠状态会按配方持久化。
- 收紧各功能页标题区的层级、间距与字号，为主要内容腾出更多可视空间。
- 侧边栏设置入口旁新增 GitHub 仓库链接按钮。

## 2026-07-21

- 首页改为公告、使用说明、扩展能力状态和最近更新，帮助新用户理解哪些功能可以直接使用。
- 为功能页增加 `Web 可用`、`本地数据增强`、`需要本地数据`、`API Bridge 增强` 和 `需要 API Bridge` 标签。
- 将本地游戏数据与 API Bridge 的配置状态集中展示在首页，并统一使用 API Bridge 这一名称。
- 将本地游戏目录、资源缓存、更新和诊断功能从原工作台迁移到设置页。

## 2026-07-20

- 新增物品页：通过 API Bridge 浏览当前角色的背包、装备、兵装库、货币、鞍囊、雇员、收藏柜和投影台等逻辑容器。
- 支持实时数据与上次保存的库存快照并存，页面会区分当前可用、缓存和未加载状态。
- 支持容器分组、分页、物品搜索，以及搜索结果在具体物品格子上的高亮。
- 图鉴可以从已保存的物品快照更新装备持有进度。

## 2026-07-16

- 接入 API Bridge，为图鉴提供游戏内收藏解锁状态和物品来源快照。
- 新增图鉴同步流程：可以从游戏状态读取解锁信息，也可以将物品数据带来的持有状态应用到图鉴。
- 改进武器模型加载和染色预览，补充模型资源加载进度与异常处理。
- 修复 Web 端物品图标在资源 ID 变化或加载失败时的回退与刷新问题。

## 2026-07-14

- 图鉴搜索支持跨分类匹配，并在 Tab、区块名称和具体格子上显示命中高亮。
- 改进图鉴分类、进度导航和完成状态展示。
- 重做图鉴目录与解锁状态存储，补充本地数据源、资源缓存和图鉴状态文档。

## 2026-07-13

- 完善装备图鉴目录，整理稳定分类和装备条目范围。
- 增加图鉴目录元数据和更紧凑的页面布局。

## 2026-07-10

- 建立类型化的图鉴目录和资源缓存体系。
- 引入 IndexedDB 资源缓存，并支持内置资源与本地游戏数据源的组合使用。

## 2026-07-01

- 新增武器模型页面，可从本地 FFXIV 游戏数据读取武器模型并进行预览。
- 增加模型旋转、缩放和基础渲染控制。

## 2026-06-24

- Web 应用迁移到 Rust + Dioxus，统一页面、导航、组件和 WASM 构建流程。

## 2026-06-18

- 新增笔记页，支持本地保存的目录、页面、制作总结、材料规划和物品详情。

## 2026-06-10

- 建立 XIV Companion 的合成检索基础功能。
- 支持配方树、材料汇总、来源选择、市场估价和 Raphael 宏求解。
- 增加制作材料规划和游戏数据导出工具。
